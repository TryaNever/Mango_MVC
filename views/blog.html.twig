<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ blog.title }}</title>
</head>
<body>
    <h1>{{ blog.title }}</h1>
    <p>Auteur : {{ blog.author ?? 'Anonyme' }}</p>
    <p>{{ blog.content }}</p>

    <h2>Commentaires</h2>
    <ul>
        {% for comment in blog.comments %}
            <li>{{ comment.username }}: {{ comment.content }}</li>
        {% else %}
            <li>pas de commentaire</li>
        {% endfor %}
    </ul>

    <a href="/">Retour à la liste</a>
</body>
</html>
